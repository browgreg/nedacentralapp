import 'package:flutter/material.dart';

class CaptainResultsScreen extends StatelessWidget {
  const CaptainResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
