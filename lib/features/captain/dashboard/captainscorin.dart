import 'package:flutter/material.dart';

class CaptainScoringScreen extends StatelessWidget {
  const CaptainScoringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class CommitteeDashboardScreen extends StatelessWidget {
  const CommitteeDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class FullResultsScreen extends StatelessWidget {
  const FullResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
